/* tslint:disable:no-unused-variable */

import { By }           from '@angular/platform-browser';
import { DebugElement } from '@angular/core';
import { async, inject } from '@angular/core/testing';
import { ConfigFormPanelComponent } from './config-form-panel.component';
import { WidgetConfig, WIDGET_CONFIG_SERVICE } from '../../services';

describe('Component: MmChartSetupPanel', () => {
  it('should create an instance', () => {
    // let 
    // let widgetConfig = new WidgetConfig();


    // let component = new ConfigFormPanelComponent(widgetConfig: WidgetConfig);
    // expect(component).toBeTruthy();
  });
});
